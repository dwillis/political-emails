__pycache__/
*.pyc
.venv/
*.egg-info/
.DS_Store

# Generated site output — built by build_site.py in CI and deployed
# as a GitHub Pages artifact. Not committed to avoid repo bloat
# (yearly ZIP archives exceed GitHub's 100MB file limit).
docs/

# Email screenshots produced by screenshot_emails.py — large binaries,
# regenerated on demand from the Gmail account.
screenshots/

# Pre-computed committee export consumed by scripts/backfill_committees.py
# (multi-GB, not committed).
emails_updated_qwen35-4b.json

# Local topic-modeling experiment (large binary model + labels).
topic_model/

# Committee validation working data: gold set, FEC bulk cache, reports/queues.
state/gold/
state/fec/
state/validation/
